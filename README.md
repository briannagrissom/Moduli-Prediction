Code to predict elastic constants of compounds. 
Purpose of "evaluate_performance.py": to train the machine learning pipeline and evaluate its performance on testing data using K-fold cross validation.
